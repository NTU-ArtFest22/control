<!DOCTYPE html>
<html ng-app="profile">
  <head>
    <meta charset="utf-8">
    <title>22nd-NTUAF-異識流 </title>

    <script data-require="angular.js@1.5.0" data-semver="1.5.0" src="https://ajax.googleapis.com/ajax/libs/angularjs/1.5.0/angular.js"></script>
    <script src="/javascripts/ng-profile.js"></script>
  </head>

  <body ng-controller="ActivityCtrl">

    <%- include layout/navbar %>
    <br>
    <br>
    <br>
    <br>
    <% if( typeof error !== 'undefined') { %>
      <div class="alert alert-danger" role="alert">
        <strong>Oh snap!</strong> Change the inviting code or selected activity and try submitting again.
      </div>
    <% } %>

    <% if( typeof warning !== 'undefined' ){ %>
      <div class="alert alert-danger" role="alert">
        <strong>Sorry!</strong> 活動還沒開始呦！
      </div>
    <% } %>


    <h4>已登記活動</h4>

    <br>
    <div class="container">
      <div class="jumbotron" ng-repeat="act in user.activities" ng-show="act.isRunning">
        <div class="row">
          <h1>{{ act.gameName }}</h1>
          <h3>{{((act.acttype==1)?'Beyond The Map@寶藏巖':((act.acttype==2)?'Beyond The Map@大稻埕':'Ear Wrom'))}}</h3>
          <h4>{{ act.time | date:'shortTime' }}, {{ act.time | date:'fullDate' }} {{ act.player_id }} @ {{ act.location }}</h4>
          <td><a class="btn btn-primary" ng-href="/profile/{{ act.id }}/{{ act.player_id }}" ng-show="act.isRunning">Go</a></td>
        </div>
      </div>
    </div>


    <%- include layout/fb-like %>
    <%- include layout/links %>

  </body>
</html>

